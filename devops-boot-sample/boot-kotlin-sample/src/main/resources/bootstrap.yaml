service:
  prefix: devops-
spring:
  application:
    name: kotlin-sample
  cloud:
    consul:
      discovery:
        metadata:
          env: aaa
server:
  port: 8080
devops:
  loadbalancer:
    gray:
      enabled: true
      meta-key: env
    local-prior:
      enabled: true
